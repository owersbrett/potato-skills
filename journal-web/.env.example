# Where journal entries (contract-valid Markdown) live.
# Absolute path, or relative to the journal-web project root.
# Default when unset: ../context-journal/out
JOURNAL_CONTENT_DIR=../context-journal/out
